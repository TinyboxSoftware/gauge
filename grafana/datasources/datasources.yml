apiVersion: 1

# Mark provisioned data sources for deletion if they're removed from this file
prune: false

datasources:
  - name: Gauge Database
    type: PostgreSQL
    access: proxy
    orgId: 1
    uid: psql-gauge
    isDefault: true
    url: $DATABASE_HOST:$DATABASE_PORT
    database: $DATABASE_NAME
    user: $DATABASE_USER
    secureJsonData:
      password: $DATABASE_PASSWORD
    jsonData:
      sslMode: disable
      connMaxLifetime: 14400
      maxOpenConns: 100
